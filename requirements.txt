# --- UI ---
streamlit>=1.64
pillow>=10.0
numpy          # mock heatmap + sample leaf drawings (riceguard_ui/mock.py)

# --- Trained models (riceguard_ui/model.py) ---
# The versions the app was tested with. TensorFlow has no Python 3.14 build, so on 3.14 these are skipped and the
# app shows mock results. Streamlit Community Cloud: choose Python 3.12 under Advanced settings.
tensorflow-cpu==2.21.0; python_version < "3.14"
keras==3.15.1; python_version < "3.14"
tf-keras-vis==0.8.7; python_version < "3.14"

# --- Training only (Colab notebooks), not needed by the app ---
# opencv-python-headless
# scikit-learn
# matplotlib
